export default function FileLayout({ children }) {
  return children;
}

export const metadata = {
  title: "File Scanner | Deep Phishing",
  description:
    "Scan files, URLs, and IP addresses to detect potential security threats and phishing attempts",
  keywords: "security scan, file scan, URL scan, IP scan, phishing detection",
};
